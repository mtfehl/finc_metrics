---
title: "Problem Set 01"
description: "Transformation into Stationary Time Series"
format: html
---

```{r set_up, include=FALSE}
setwd("C:/Users/T14/7Programming/R/class/finc_ecm/problem_sets/HW_1_Transformations")
packages <- c("tidyverse",
              "xts",
              "tsibble",
              "zoo")

# for more info on these ts packages:
# vignette("zoo")
# vignette("xts")
# vignette("tsibble")

for (i in packages){
  if (!require(i, character.only = T)){
    install.packages(i)
    library(i, character.only = T)
  } 
  else
    library(i, character.only = T)
}
```

# Problem Set 1: trans

```{r chunk1}
print("test")

```
